package tantros.type.buildConfig;

import tantros.type.blockConfig.BlockConfig;

public class WarmupConfig implements BlockConfig {

    public float warmupSpeed = 0.019f;

    public boolean warmupTargetsEfficiency = false;

}
