package tantros.type.buildConfig;

import tantros.type.blockConfig.BlockConfig;

public class FlywheelConfig implements BlockConfig {

    public float maxSpeed = 1;
    public float inertia = 2000f;

    public String flywheelStateName;
}
